import React, { useState } from 'react';
import { FormProps } from '../types'; 

const Branding: React.FC<FormProps> = ({
  formData,
  setFormData,
  errors,
  setErrors,
  loading,
  setLoading,
}) => {

  return (
    <div className="p-4">
    {/* Replace 'forms' with your actual form components */}
    forms
  </div>
  );
};

export default Branding;
