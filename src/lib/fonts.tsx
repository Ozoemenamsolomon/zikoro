import { Heebo, Manrope } from "next/font/google";

export const heebo = Heebo({
  subsets: ["latin"],
  variable: "--font-heebo",
  display: "swap",
});

export const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
});
