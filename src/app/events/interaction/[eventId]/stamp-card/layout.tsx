
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "StampCard",
  description: "List of partners with stamp card",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
