export interface Event {
    name: string;
    type: string;
    startDate: string
    endDate: string
    startTime: string
    endTime: string
    location: string
    participants: string
    price: number
    publishedDate: string
}